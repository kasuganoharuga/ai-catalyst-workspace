// Scaffold only — no server, tools, or business logic yet. This file exists
// solely so `tsc --noEmit` has an input; the real Streamable HTTP server
// (architecture.mdc rule 3) is a follow-up implementation pass.
export {};
