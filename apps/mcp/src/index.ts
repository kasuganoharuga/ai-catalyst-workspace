// Scaffold only — no server, tools, or business logic yet. This file exists
// solely so `tsc --noEmit` has an input; the real Streamable HTTP server is
// a follow-up implementation pass.
export {};
