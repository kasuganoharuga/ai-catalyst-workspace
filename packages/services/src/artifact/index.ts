// Scaffold only — no implementation yet. Will own Artifact submission,
// rendering, and validation logic. Official artifact validation is never
// exposed as an MCP tool (schema check constraints already gate it); MCP
// only ever exposes the non-authoritative run_draft_check path.
export {};
