// Scaffold only — no implementation yet. Once built, this will be the sole
// entry point packages/services uses to call apps/api, per architecture.mdc
// rule 2 (FastAPI is internal-only; called from packages/services, never
// from apps/web or apps/mcp directly).
export {};
